
apiVersion: v1
kind: Service
metadata:
  name: aks-demo-app-service
  labels:
    app: aks-demo-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: aks-demo-app
